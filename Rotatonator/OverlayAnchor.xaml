<Window x:Class="Rotatonator.OverlayAnchor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overlay Position" 
        Height="80" Width="200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Left="100" Top="100"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <Border Background="#99000000" 
            CornerRadius="10" 
            Padding="15"
            BorderBrush="#4CAF50"
            BorderThickness="2">
        <StackPanel>
            <TextBlock Text="📍 Overlay Position" 
                       FontSize="14" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Drag to position" 
                       FontSize="10" 
                       Foreground="#AAAAAA" 
                       HorizontalAlignment="Center"
                       Margin="0,3,0,0"/>
        </StackPanel>
    </Border>
</Window>
